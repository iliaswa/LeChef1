<h1>This is your show page</h1>
<<%= @user.first_name %>>
<p><%= @chef.details %></p>
<p><%= @chef.cuisine %></p>

<%= link_to "Delete",
chef_path(@chef), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to "Edit",
  edit_chef_path(@chef)
%>

<%= link_to "New",
  new_chef_path(@chef)
%>
